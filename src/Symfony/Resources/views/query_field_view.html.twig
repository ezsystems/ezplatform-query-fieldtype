{% for result in query_results %}
    {{ render(controller("ez_content:viewAction", {
        "contentId": result.id,
        "viewType": parameters['result_view_type']|default(content_query_line_view)
    })) }}
{% endfor %}
